# E2E test values - minimal settings for Kind cluster testing
image:
  repository: garage-operator
  tag: e2e
  pullPolicy: Never

leaderElection:
  enabled: true

# Disable security contexts for Kind cluster compatibility
podSecurityContext: {}
securityContext: {}

# No resource limits for tests
resources: {}
