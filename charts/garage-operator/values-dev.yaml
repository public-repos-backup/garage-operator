# Development values - for local testing with Kind
image:
  repository: garage-operator
  tag: dev
  pullPolicy: Never

leaderElection:
  enabled: true

# Disable security contexts for Kind cluster compatibility
podSecurityContext: {}
securityContext: {}

# No resource limits for dev
resources: {}
